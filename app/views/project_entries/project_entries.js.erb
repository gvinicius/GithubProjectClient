function errorHandling(language){
  $('#about-search').text('No matches found for "' + language + '" or server unavailable')
}

function alertOld(language){
  $('#about-search').text('There are enough results for "' + language + '" here.')
}
